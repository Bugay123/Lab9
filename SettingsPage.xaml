<Page
    x:Class="Lab9.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lab9"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="59,43,0,0" TextWrapping="Wrap" Text="Settings" VerticalAlignment="Top" FontSize="24" FontFamily="Arial Black"/>
        <RadioButton x:Name="radioButton" IsChecked="{x:Bind ViewModel.IsByTitle, Mode=TwoWay}" Content="By title" Margin="93,147,0,0" VerticalAlignment="Top"/>
        <RadioButton x:Name="radioButton1" IsChecked="{x:Bind ViewModel.IsByArtist, Mode=TwoWay}" Content="By artist" Margin="93,184,0,0" VerticalAlignment="Top"/>
        <RadioButton x:Name="radioButton2" IsChecked="{x:Bind ViewModel.IsByAlbum, Mode=TwoWay}" Content="By album" Margin="93,221,0,0" VerticalAlignment="Top"/>
        <RadioButton x:Name="radioButton3" IsChecked="{x:Bind ViewModel.IsByYear, Mode=TwoWay}" Content="By year" Margin="93,258,0,0" VerticalAlignment="Top"/>
        <CheckBox x:Name="isSorted" IsChecked="{x:Bind ViewModel.IsSorted, Mode=TwoWay}" Content="Sorting" Margin="93,96,0,0" VerticalAlignment="Top"/>
        
    </Grid>

</Page>
